NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
# Token server UploadThing (foto bukti jurnal). Isi dari uploadthing.com → API Keys.
# Tanpa token: upload foto otomatis fallback ke Firebase Storage → dataURL lokal.
UPLOADTHING_TOKEN=
